import { GetFirst50CharPipe } from './get-first50-char.pipe';

describe('GetFirst50CharPipe', () => {
  it('create an instance', () => {
    const pipe = new GetFirst50CharPipe();
    expect(pipe).toBeTruthy();
  });
});
